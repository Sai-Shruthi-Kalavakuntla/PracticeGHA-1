name: demo env
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  env_demo:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: Connecting to Env
        env:
          APP_ENV: "prod"
        run: echo "This workflow runs on $APP_ENV"
