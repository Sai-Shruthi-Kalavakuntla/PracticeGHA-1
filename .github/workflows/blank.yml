name: PracticeGHA
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build setup
        run: echo "Build setup completed"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Tests setup
        run: echo "Tests setup completed"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment setup
        run: echo "Deployment setup completed"
    
      
