name: PracticeGHA
on: ["push", "pull_request"]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build setup
        run: echo "A demo Build"
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Tests setup
        run: echo "Tests setup completed"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Deployment setup
        run: echo "Deployment setup completed"
    
      
